# ~/.config/zsh/private.zsh - 机密环境变量 (权限 600,勿提交/勿分享)
# 使用方法: cp private.zsh.example ~/.config/zsh/private.zsh && chmod 600 ~/.config/zsh/private.zsh
# 然后去 https://serper.dev 重新生成 key 并替换下面占位符 (旧 key 已泄漏,视为作废)。
export SERPER_API_KEY="YOUR_SERPER_API_KEY_HERE"
